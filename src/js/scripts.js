( function( root, $, undefined ) {
	'use strict';

	$( function() {

		// DOM ready, take it away
		/*var m = new Marka( '#menu_icon' );
		m.set( 'bars' ).size( 40 );*/
		console.log( 'tlalala' );
	});
} ( this, jQuery ) );
